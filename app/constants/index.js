const { Role } = require('./role')
const { Status, resData } = require('./status')
const { Rank } = require('./rank')
const KeyConst = {
    RoleConst: Role,
    Status,
    RankConst: Rank,
    resData
}
module.exports = {
    KeyConst
}