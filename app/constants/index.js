const { Role } = require('./role')
const { Status } = require('./status')
const { Rank } = require('./rank')
const KeyConst = {
    Role,
    Status,
    RankConst: Rank
}
module.exports = {
    KeyConst
}